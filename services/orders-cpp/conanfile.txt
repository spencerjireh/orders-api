# Dependency manifest for the C++ orders service. Empty by design: the service
# builds with only a compiler and CMake, and this file exists so a PR that
# adds a dependency is a manifest change and detonates.
[requires]

[generators]
